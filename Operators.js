/*Arithmetic operators
Assignment operators
Comparison operators
Logical operators
Type operators
Bitwise operators */

/* Arithmetic operators */
/*
+
-
*
/
**
++
--
*/

/*Assignment operators*/
// = assign
// += add and assign
// -= subtract and assign
// /= divide and assign
// *= multiply and assign
// %= modulus and assign
// **= exponential and assign

// let x= 20;

// x +=10;
// console.log('The value of x is: '+x)

/* Comparison operators */
// == only checks the value
// === checks value and type
// != not equals to value
// !== not equals to value and type
// >= greater than or equal to
// <= less than or equal to
// ? ternary operator
/* like if-then-else
takes 3 arguments 
condition ? value if true : value if false */

// let x = 15;
// let y =20;

// let result = x < y ? x : y;
// console.log(result);

// Logical operators
/* && AND returns true if both conditions are true
   || OR  returns true if one of the conditions is true
   ! NOT  returns true if the condition is false */

   // let a = true;
   // let b = false;

   // console.log(a);

   /* Type Operators */

   // typeof: Returns data type of the variable

   let carName = 'Toyota';
   console.log(carName);

   //instanceof: returns true if an object is an instance of object type
   // it returns a boolean value that indicates if an object is an instance of a particular class


   
   /* Bitwise Operators */
   // & AND = sets bits to 1 only if both are 1
   // | OR = sets bits to 1 even if only one bit is 1
   // ^ XOR = sets bits to 1 only if one is 1 if both are 1 it will be 0 
   // ~ NOT = inverts all the bit {it is a single operator i.e you don't need two value input}
   // << Left Shift = shifts left and fills all the spaces with 0
   // >> Right Shift = shifts right